# Copy to android/key.properties (gitignored) and fill in real values.
# The keystore must stay outside the repo; use an absolute storeFile path.
storePassword=your-store-password
keyPassword=your-key-password
keyAlias=musallam_delivery
storeFile=/Users/wb/musallam-release.jks
